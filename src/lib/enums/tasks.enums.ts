export enum TaskPriorityEnum {
  High = "high",
  Medium = "medium",
  Low = "low",
}

export enum TaskStatusEnum {
  Opened = "opened",
  Progress = "progress",
  Completed = "completed",
  Cancelled = "cancelled",
}
